import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const H={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json"};const URL=Deno.env.get("SUPABASE_URL")??"",KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"",AI=Deno.env.get("GEMINI_API_KEY")??"";const json=(x:unknown,s=200)=>new Response(JSON.stringify(x),{status:s,headers:H});
const empty=(m:string)=>({ok:false,detectedInstrument:null,detectedInstrumentLabel:null,broker:null,timeframe:null,chartReadable:false,direction:"WAIT",confidence:0,methodology:{higherTimeframeDirection:"Not confirmed",m30Confirmation:"Not confirmed",resistanceSupportRbs:"Not confirmed",engulfing:"Not confirmed",lowerTimeframeConfirmation:"Not confirmed",trendlinePriceAction:"Not confirmed",structuralInvalidation:"Not confirmed",continuationManagement:"Not confirmed"},feedback:m,warnings:["No decision was fabricated from an unreadable or unsupported screenshot."],evidence:[]});
async function user(token:string){const r=await fetch(`${URL}/auth/v1/user`,{headers:{apikey:KEY,Authorization:`Bearer ${token}`}});return r.ok?r.json():null;}
async function allowed(id:string){const r=await fetch(`${URL}/rest/v1/rpc/smartvibe_user_has_scanner_access`,{method:"POST",headers:{apikey:KEY,Authorization:`Bearer ${KEY}`,"Content-Type":"application/json"},body:JSON.stringify({p_user_id:id})});return r.ok&&await r.json()===true;}
const prompt=`You are SmartVibe Trading Network's visual chart-analysis engine. Analyze ONLY the uploaded screenshot. The screenshot is the source of truth for the instrument. Never substitute a user-selected/default instrument. Gold/XAUUSD remains Gold; Nasdaq/NAS100/US100 remains Nasdaq. Normalize broker aliases but preserve the visible symbol in evidence. Identify Deriv, MT5, TradingView or another platform only when visible. If instrument cannot be read reliably, detectedInstrument=null and WAIT.

Canonical SmartVibe methodology: H1 direction -> M30 confirmation -> support/resistance/RBS -> EG BUY/EG SELL zone -> M5/M1 confirmation -> trendline/price-action confirmation -> BUY/SELL/WAIT -> structural invalidation -> next major support/resistance -> MANAGE, DON'T CAP THE MOVE. EG BUY=bullish engulfing; EG SELL=bearish engulfing. Liquidity, FVG, OB, CRT/session structure and 714 are confluence only. 150-200 pips is a management milestone, never a hard cap; valid 300/400/600+ continuation remains eligible while methodology is valid. Never invent unreadable prices. Do not promise profits. Return ONLY JSON with fields: ok,detectedInstrument,detectedInstrumentLabel,broker,timeframe,chartReadable,direction(BUY|SELL|WAIT),confidence(0-100),methodology{higherTimeframeDirection,m30Confirmation,resistanceSupportRbs,engulfing,lowerTimeframeConfirmation,trendlinePriceAction,structuralInvalidation,continuationManagement},feedback,warnings[],evidence[].`;
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:H});if(req.method!=="POST")return json({error:"POST required"},405);if(!AI)return json({...empty("The visual scanner is awaiting its server-side AI provider key."),status:"provider-pending"},503);const token=(req.headers.get("Authorization")??"").replace(/^Bearer\s+/i,"").trim();if(!token)return json({error:"Authentication required."},401);try{const u=await user(token);if(!u?.id)return json({error:"Authenticated user could not be verified."},401);if(!(await allowed(u.id)))return json({error:"Chart Scanner is available on Advanced and Premium Pro subscriptions only."},403);const b=await req.json();const image=String(b?.imageBase64??"").replace(/^data:[^;]+;base64,","");const mime=String(b?.mimeType??"image/jpeg").toLowerCase();if(!image)return json({error:"Chart screenshot is required."},400);if(!["image/jpeg","image/png","image/webp"].includes(mime))return json({error:"Use JPEG, PNG or WEBP."},400);if(image.length>12000000)return json({error:"Screenshot is too large."},413);const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-3.1-flash-lite:generateContent?key=${AI}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:prompt},{inline_data:{mime_type:mime,data:image}}]}],generationConfig:{temperature:.1,responseMimeType:"application/json"}})});const d=await r.json();const raw=d?.candidates?.[0]?.content?.parts?.find((p:any)=>p?.text)?.text;if(!r.ok||!raw)return json({...empty("The visual AI provider did not return usable analysis."),status:"unavailable"},503);let out:any;try{out=JSON.parse(raw)}catch{return json({...empty("Malformed visual analysis. No trade decision was created."),status:"unavailable"},503)}const instrument=typeof out?.detectedInstrument==="string"&&out.detectedInstrument.trim()?out.detectedInstrument.trim():null;const direction=["BUY","SELL","WAIT"].includes(out?.direction)?out.direction:"WAIT";out={...out,ok:!!instrument&&!!out?.chartReadable,detectedInstrument:instrument,direction:!instrument||!out?.chartReadable?"WAIT":direction,confidence:Math.max(0,Math.min(100,Number(out?.confidence)||0)),warnings:Array.isArray(out?.warnings)?out.warnings.map(String).slice(0,8):[],evidence:Array.isArray(out?.evidence)?out.evidence.map(String).slice(0,10):[]};return json(out)}catch(e){return json({...empty("The SmartVibe scanner is temporarily unavailable. No signal was fabricated."),status:"unavailable"},503)}});
