import React,{createContext,useContext,useMemo,useState} from 'react';
import {Instrument,instruments} from '@/data/instruments';
export type Position={id:string;symbol:string;side:'BUY'|'SELL';size:number;entry:number;sl:number;tp:number;openedAt:string};
export type Event={id:string;title:string;detail:string;time:string;amount:number};
type Ctx={selectedInstrument:Instrument;setSelectedInstrument:(i:Instrument)=>void;selectedStrategy:string;setSelectedStrategy:(s:string)=>void;selectedSession:string;setSelectedSession:(s:string)=>void;selectedStyle:string;setSelectedStyle:(s:string)=>void;consensusThreshold:number;setConsensusThreshold:(n:number)=>void;positions:Position[];events:Event[];placePaperTrade:(side:'BUY'|'SELL',size:number,entry:number,sl:number,tp:number)=>void;closePosition:(id:string)=>void};
const TradingContext=createContext<Ctx|null>(null);
export function TradingProvider({children}:{children:React.ReactNode}){const [selectedInstrument,setSelectedInstrument]=useState(instruments.find(x=>x.symbol==='XAUUSD')!);const [selectedStrategy,setSelectedStrategy]=useState('CRT Range Reversal');const [selectedSession,setSelectedSession]=useState('London');const [selectedStyle,setSelectedStyle]=useState('Day Trading');const [consensusThreshold,setConsensusThreshold]=useState(6);const [positions,setPositions]=useState<Position[]>([{id:'seed',symbol:'XAUUSD',side:'SELL',size:.05,entry:3340,sl:3360,tp:3300,openedAt:new Date().toISOString()}]);const [events,setEvents]=useState<Event[]>([{id:'e1',title:'Position opened',detail:'XAUUSD SELL 0.05 lots',time:'Today',amount:0},{id:'e2',title:'Position closed',detail:'EURUSD BUY',time:'Yesterday',amount:42}]);const placePaperTrade=(side:'BUY'|'SELL',size:number,entry:number,sl:number,tp:number)=>{const id=Date.now().toString();setPositions(p=>[...p,{id,symbol:selectedInstrument.symbol,side,size,entry,sl,tp,openedAt:new Date().toISOString()}]);setEvents(e=>[{id:'ev'+id,title:'Position opened',detail:`${selectedInstrument.symbol} ${side} ${size} lots`,time:'Just now',amount:0},...e]);};const closePosition=(id:string)=>{const p=positions.find(x=>x.id===id);if(!p)return;setPositions(x=>x.filter(y=>y.id!==id));setEvents(e=>[{id:'ev'+Date.now(),title:'Position closed',detail:`${p.symbol} ${p.side}`,time:'Just now',amount:Math.round((Math.random()-.35)*100)},...e]);};return <TradingContext.Provider value={{selectedInstrument,setSelectedInstrument,selectedStrategy,setSelectedStrategy,selectedSession,setSelectedSession,selectedStyle,setSelectedStyle,consensusThreshold,setConsensusThreshold,positions,events,placePaperTrade,closePosition}}>{children}</TradingContext.Provider>}
export const useTrading=()=>{const c=useContext(TradingContext);if(!c)throw new Error('useTrading must be inside TradingProvider');return c};
