import {ScrollView,Text,Pressable} from 'react-native';import {useTrading} from '@/context/TradingContext';import {Card,Header,Screen,C} from '@/components/ui';const names=['CRT Range Reversal','AMD Session Cycle','Turtle Soup / KOD','Validated Order Block','Malaysian SNR','Trendline Break and Retest','FVG Retest','Falcon Nature Structure'];export default function Strategies(){const {selectedStrategy,setSelectedStrategy}=useTrading();return <Screen><ScrollView><Header eyebrow="PAPER STRATEGY LIBRARY" title="Choose one method"/><Card><Text style={{color:C.amber,fontWeight:'800'}}>Safety note: strategy signals are educational and simulated.</Text></Card>{names.map(n=><Pressable key={n} onPress={()=>setSelectedStrategy(n)}><Card><Text style={{fontSize:17,fontWeight:'900'}}>{selectedStrategy===n?'◉ ':'○ '}{n}</Text><Text style={{color:selectedStrategy===n?C.green:C.muted,marginTop:5}}>{selectedStrategy===n?'ACTIVE':'Available'}</Text></Card></Pressable>)}</ScrollView></Screen>}
